import 'package:flutter/material.dart';

const kLargeScreenPadding = EdgeInsets.symmetric(horizontal: 100, vertical: 40);
const kMediumScreenPadding = EdgeInsets.symmetric(horizontal: 50, vertical: 20);
const kSmallScreenPadding = EdgeInsets.symmetric(horizontal: 20, vertical: 20);
